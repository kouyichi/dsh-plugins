# dsh 插件全家桶二期 — 生态调研 × 6 Agent 能力矩阵 × 8 新插件开发实测 × 全家桶深度 Review 报告

**日期**：2026-08-16 ｜ **执行**：Hermes Agent（本机） ｜ **工期**：约 7 小时
**代码**：`/workspace/algorithm/dsh-plugins/`（git：86fd630 → 7760cf9 → 35521e3 → 929abbd → 4600c6d）
**调研报告存档**：`/workspace/algorithm/hermes-work/dsh-plugin-ecosystem-report.md`（32KB）、`/workspace/algorithm/hermes-work/research/capability-matrix-ai-coding-agents.md`（34KB）

---

## 0. 任务总览

| 阶段 | 内容 | 产出 |
|---|---|---|
| 1. 生态调研 | GitHub 上 dsh 插件生态全景（官方 97 包 + 第三方 4597 topic 仓库 + awesome 列表 + npm） | dsh-plugin-ecosystem-report.md |
| 2. 能力调研 | hermes / codex / claude-code / pi / opencode / kimicode 六 agent 能力矩阵 | capability-matrix-ai-coding-agents.md |
| 3. 本机盘点 | 5 个既有插件（learn/profile/dream/tower/kanban）+ dsh-tui-app + 内置 ~150 插件 | 见 §5 |
| 4. 方向决策 | 基于「生态空白 × 可借鉴能力 × 本机已验证 API」确定 8 个新插件 | §3 |
| 5. 开发+验证+实测 | 8 个新插件全部真实跑通（含 3 轮修复迭代） | §3、§6 |
| 6. 深度 review | 既有插件静态审查 + 全量实测 + tui-app PTY 实测 | §5 |

---

## 1. 调研成果一：dsh 插件生态现状（关键结论）

> 详细数据见 `dsh-plugin-ecosystem-report.md`（所有数字来自 2026-08-16 实时 GitHub/npm 查询）。

### 1.1 基本盘
- **deepseek-ai/deepseek-harness**：2026-08-13 开源（developer preview，MIT，TypeScript），3 天 122k+ star / 12k fork，rc.6 仍在 breaking change。
- **官方 = 内核**：97 个 `@deepseek-ai/dsh-*` npm 包全部是抽象 seam/默认实现/UI 组件；官方刻意不做用户功能插件（唯一官方 skill 是个 badge）。用户功能全留给第三方。
- **第三方 = 淘金热早期**：`dsh-plugin` topic 已有 **4,597 个仓库**（几乎全是开源 3 天内创建）；npm 第三方包 15+；awesome 列表 25+；插件市场 6+。

### 1.2 卷 vs 空白（方向决策的直接依据）

**最卷（明确避开）**：Web UI 增强/主题（100+，仅余额计费组件就 20+ 重复）、视觉/多模态桥（8+，modlens 2.1k★）、TUI（ccch1mneyyy/dsh-TUI 1.4k★ 一家独大）、桌面壳（7.4k★ 领跑）、插件市场（6+）、记忆（10+ 但碎片化、无标准）。

**🔥 显著空白**（实测数据，非推测）：
| 方向 | 现有覆盖 | 本机动作 |
|---|---|---|
| 任务看板闭环 | 8 个仓库全部 ≤3★，且都是 Web UI 小部件，无 agent 分发闭环 | 已有 dsh-kanban（SQLite+dispatcher+子代理执行，**实测领先全生态**） |
| 安全/审批/治理 | 全 0-3★ 萌芽（smart-approval 0★、autogate 1★） | **dsh-guard（本期）** |
| 插件开发工具链 | 仅 skill 级（≤21★），无脚手架/测试/兼容性检测 | **dsh-scaffold（本期）** |
| 测试/benchmark | 仅 1★ 仓库，官方 BENCHMARK.md 面向内核 | **dsh-bench（本期）** |
| 会话数据深度分析/导出 | 半空白（计费组件偏 Web UI，无会话仓库） | **dsh-xray + dsh-meter（本期）** |
| 定时后台任务 | 官方 schedule 是会话内提醒；第三方稀疏 | **dsh-cron（本期）** |
| 上下文管理 | 无 | **dsh-pack（本期）** |
| A2A 服务端 | 完全空白（官方示例仅 ACP/JSON-RPC） | **dsh-a2a（本期）** |

---

## 2. 调研成果二：六大 AI Coding Agent 能力矩阵（关键结论）

> 详细报告见 `research/capability-matrix-ai-coding-agents.md`。

### 2.1 pi 身份确认（调研中第一个要解决的问题）
**任务里的 "pi" = `earendil-works/pi`（Pi Agent Harness，91k★，TypeScript，MIT）**，不是 DeepSeek 的产品。证据链：
1. GitHub `earendil-works/pi` 自述 "AI agent toolkit: unified LLM API, agent loop, TUI, coding agent CLI"；核心三包 `@earendil-works/pi-coding-agent` / `pi-agent-core` / `pi-ai`。
2. dsh 自己的 `dsh-llm-pi-ai` 插件（本机 node_modules 实证）的依赖正是 `@earendil-works/pi-ai ^0.82.1`——dsh 用 pi-ai 作为接入 DeepSeek 模型的 LLM 后端适配层。
3. deepseek-ai GitHub 组织下没有 pi 产品仓库；npm `pi-ai` 是 mitsuhiko 的占位包（排除）。

### 2.2 六 agent 能力矩阵（摘要）

| Agent | Repo / ★ | 独特能力（dsh 可借鉴） | 本期落地 |
|---|---|---|---|
| Hermes | NousResearch/hermes-agent / 231k | 技能自改进学习闭环（curator）；cron；多平台网关；**A2A 平台插件 + ACP server** | A2A→dsh-a2a；curator 已移植 learn |
| Codex | openai/codex / 106k | **sandbox mode × approval policy 双层安全 + 域名白名单**；记忆 consolidation；**技能 2% 上下文预算渐进披露** | guard 规则层；pack_budget |
| Claude Code | anthropics/claude-code / 141k | **PreToolUse hook 安全门**（security-guidance）；插件=commands+agents+skills+hooks+MCP 打包；多 agent 并行评审 | guard 审计+拒绝 |
| Pi | earendil-works/pi / 91k | TS 扩展事件总线（tool_call 可阻断/context 可改写/jiti 热重载）；订阅制 provider；JSONL 树形会话 fork/clone | （事件订阅模式同构） |
| OpenCode | anomalyco/opencode / 198k | **TUI=server 客户端 + OpenAPI 自描述**；可编程权限事件 permission.asked/replied | （A2A/JSON-RPC 端点同构） |
| Kimi Code | MoonshotAI/kimi-code / 6.8k | **内置 cron（CronCreate/List/Delete）**；sessionStart.skill + systemPrompt 注入；AgentSwarm；**/tower（2026-08-16 上游合并 #2633）** | cron→dsh-cron；tower v2（用户并行会话已移植） |

---

## 3. 8 个新插件：方向决策 → 开发 → 验证 → 实测

**决策原则**：生态空白（§1.2）× 六 agent 可借鉴能力（§2）× 本机已验证 rc.6 API × 可在 headless 下产出真实证据。所有插件挂在 `~/.dsh/cordis.patch.yml` 家级 patch（13 个插件，全 profile 生效），`install.sh` 幂等安装。

### 3.1 dsh-scaffold — 插件开发工具链（🔥空白：无脚手架/测试/兼容性检测）
- **借鉴**：Claude/Kimi 插件打包规范 + Codex $skill-creator。
- **工具**：`scaffold_plugin`（生成 rc.6 规范骨架：package.json/index.js/verify.mjs/README）、`verify_plugin`（mock-ctx 加载插件，逐工具检查 output.{schema,render}/presentCall/inject 契约）、`compat_check`（检测安装的 dsh 版本 + 扫描 rc.6 已知坑：presentResult 失效、ctx.interval 缺 timer inject、register 缺 output；**带注释/字符串剥离器防误报**）。
- **实测**：生成 demo-tool 骨架 → verify_plugin 通过 → compat_check 干净（0 问题，修复后）。

### 3.2 dsh-guard — 安全/审批/治理（🔥空白：整个分类 0-3★）
- **借鉴**：Codex sandbox×approval + Claude PreToolUse hook（security-guidance）。
- **机制**：`tools.guard()` 官方 seam 注册单调拒绝规则（tool 名精确/通配 + 参数正则），命中即拒绝并把理由回给模型；`ctx.on("session/event")` 全量审计（tool/call + tool/result isError）落盘 `~/.dsh/guard/audit.jsonl`。
- **工具**：`guard_rules`（add/remove/list/toggle）、`guard_report`（工具分布/错误率/被拒统计）、`guard_status`、`guard_export`（markdown）、`guard_clear`。默认零规则=零拦截。
- **实测**：添加 `bash ~ GUARD-DENY-MARKER` 规则后，`echo GUARD-DENY-MARKER` 被真实拒绝（`Error: [guard] 测试拦截规则`），正常命令放行，审计记录 177 条、被拒事件正确入报告。

### 3.3 dsh-xray — 会话数据深度分析/导出（⚠️半空白）
- **机制**：多帧 zstd 解码 + 紧凑 JSON 容错解析（复用 dsh-ops skill 踩坑成果），纯本地分析 `~/.dsh/sessions/`。
- **工具**：`xray_sessions`（轮次/步骤/工具分布/错误率/Token/按天）、`xray_session`（单会话深挖：用户消息/助手摘要/失败明细）、`xray_search`（跨会话全文搜索）、`xray_export`（markdown/JSON 导出，归档或喂给其他 agent）。
- **实测**：50 会话分析出 21 轮/57 步/29 工具调用/0 失败；导出最近会话 23KB markdown 落盘。

### 3.4 dsh-cron — 后台定时任务（差异化：官方 schedule 是会话内提醒）
- **借鉴**：Hermes cron + Kimi 内置 cron。
- **机制**：持久任务（`~/.dsh/cron/jobs.json` + logs.jsonl），30s tick 调度；**每个任务用插件自主创建的独立 agent 执行**（agents.create + installModelSelection + presets.mount try/catch——不依赖任何活会话），结果摘要落日志，重启后按 next_run_at 继续。
- **工具**：`cron_add/list/remove/run/logs/status`。
- **实测**：cron_add → cron_run 真实执行 bash（echo CRON-REAL-OK 输出成功）→ cron_logs 记录 ok/4s；两次早期失败（缺依赖、ctx.off）已修复并在日志中留痕。

### 3.5 dsh-bench — Agent 评测/Benchmark（🔥空白）
- **机制**：任务数组/JSON 文件 → 每任务独立 agent 执行 → expect 关键词判 pass/fail → 时长统计 → markdown 报告落盘 `~/.dsh/bench/runs/`。
- **工具**：`bench_run`、`bench_list`、`bench_report`。
- **实测**：2 任务（bash 输出标记 + 纯文本）**2/2 通过**，报告/历史正确。

### 3.6 dsh-pack — 上下文打包（借鉴 Codex 2% 预算 + Pi resources + Claude CLAUDE.md）
- **工具**：`pack_build`（文件摘录+技能目录+MEMORY.md+最近会话要点 → 预算受限 markdown 包）、`pack_list`、`pack_show`、`pack_budget`（技能目录上下文占用 vs 模型窗口的 2% 规则检查，实测读真实模型 contextWindow）。
- **实测**：构建 smoke-pack（1826 字符/预算 6000，含 MEMORY.md 摘录与技能目录）；pack_budget 正常（技能目录当前为空 → 0%）。

### 3.7 dsh-a2a — 原生 A2A v1.0 服务端（🔥空白：生态无任何 A2A server）
- **借鉴**：Hermes plugins/platforms/a2a。
- **机制**：node:http JSON-RPC 端点（默认 127.0.0.1:9917），方法 `tasks/send|get|cancel|list` + `agent/get`（AgentCard）；可选 Bearer token（`DSH_A2A_TOKEN` / require_token 自动生成）；每任务独立 agent 执行，会话落盘可审计；`DSH_A2A_AUTOSTART=1` 可自动启动。
- **实测**（curl 端到端）：a2a_start → agent/get 返回 AgentCard → tasks/send（状态 working）→ 20s 轮询 tasks/get（状态 **completed**，产物含 bash 执行结果 A2A-REAL-OK-777）→ a2a_status/a2a_tasks 确认。

### 3.8 dsh-meter — Token 用量/成本统计（模型侧空白；Web UI 侧已内卷）
- **工具**：`meter_summary`（命中率/成本/按天/Top 会话）、`meter_session`（单会话）、`meter_report`（markdown 周报）。
- **价格**：`~/.dsh/meter/pricing.json` 可覆盖（默认标注为估算）。
- **实测**：近 30 天 50 会话：输入 134.6k+822.8k 缓存读（**命中率 85.9%**），输出 11.6k，估算成本 $0.167；239 会话全量报告 $17.80 落盘。

### 3.9 开发期修复迭代（真实工程证据）
| 问题 | 根因 | 修复 |
|---|---|---|
| tower 加载失败 "tower_init must declare output" | **与用户并行工作流撞车**：dsh web 实例（:20010）里的会话在 16:41-16:53 把 dsh-tower 重写为 Kimi /tower 忠实移植（v2），我 16:54 冒烟恰逢其写文件 | 等待其完成；确认 v2 合法后继续（详见 §5.4） |
| cron/bench/a2a 执行失败 "Cannot find package @deepseek-ai/dsh-session" | ESM 按 symlink 真实路径解析，`/workspace/algorithm/dsh-plugins/` 无 node_modules | 插件根目录建 package.json 装 dsh-session/agent/llm（6s） |
| cron/bench/a2a "cannot get property off without inject" | **cordis ctx.on 返回 disposer，没有 .off()** | `const unsub = agent.ctx.on(...)` → `unsub()` |
| compat_check 误报自身 | 正则扫描命中注释/字符串里的 "presentResult" | 扫描前剥离注释与字符串字面量 |
| pack_build "NaN 个会话要点" | `Number(undefined) ?? 3` = NaN | `undefined ? 3 : ...` 显式判空 |

---

## 4. 挂载与安装（13 插件全家桶）

- `~/.dsh/plugins/` 13 个符号链接 → `/workspace/algorithm/dsh-plugins/`
- `~/.dsh/cordis.patch.yml` 家级 patch（install.sh 自动生成，13 行 insert）→ headless/tui/web 全 profile 生效
- headless/tui/web 三个 profile 的 package.json 均加 `file:` 依赖 + pnpm install + 热链
- `dsh --dump-config` 三个 profile 全 OK；`dsh --version` 0.1.0-rc.6

---

## 5. 既有插件深度 Review + 实测

### 5.1 dsh-learn（自我学习 + curator）— ✅ 实测通过，静态良好
- **实测**：record → draft → promote（技能真实落盘 `~/.dsh/skills/review-smoke/`）→ list 显示 active → retire → archived，全生命周期闭环。
- **静态审查**：结构清晰（收件箱→草案→发布→生命周期状态机）；生命周期窗口与 Hermes curator 语义一致（30 天 stale / 90 天归档 / pinned 豁免）；审查子代理 dry-run 默认安全。**发现**：①`applyAutoTransitions` dry-run 预览调用会真实修改 throwaway state 对象（无副作用，可接受）；②`learn_draft` 用 `writeFileSync(INBOX)` 整体重写而非 append，并发记录理论上有竞态（单进程实际安全）；③dsh-learn 与 dsh-dream 部分能力重叠（dream 也做会话经验提取），但定位不同（learn=显式记录→技能，dream=隐式整合→记忆），建议在 runtime skill 中互相引用。

### 5.2 dsh-profile（profile 管理）— ✅ 实测通过
- **实测**：profile_list（3 个 profile + 13 插件清单）、profile_info 正常；CLI `dsh-profile` 此前已验证。
- **静态审查**：薄封装层（index.js → core.js），职责单一；ESM 惰性 DSH_HOME 修复（7760cf9）正确。**发现**：`profile_use` 写 `~/.dsh/default-profile` + wrapper 的机制与 dsh 原生 `--profile` 参数并存，语义需在 README 明确（避免用户困惑）。

### 5.3 dsh-dream（记忆整合）— ✅ 状态实测，静态良好
- **实测**：dream_status 正常（上次整合 2026-08-15 22:03，20 会话 → 37 行 MEMORY.md）。
- **静态审查**：4-phase 流程完整；zstd 多帧解码正确；信号提取噪声过滤（system-reminder 等）到位；字符预算防爆。**发现一个真 bug**：`if (last === null && existsSync(MEMORY_FILE)) return;` —— 若 state.json 丢失但 MEMORY.md 存在（如手动备份恢复），自动 dream 将**永远被跳过**（注释自相矛盾）。建议改为 `if (last === null && !existsSync(MEMORY_FILE)) return;`（没有记忆也没有历史时才跳过？不对——首次 dream 应运行；应删掉这行或改为仅当 MEMORY.md 不存在且无 state 时跳过）。

### 5.4 dsh-tower（塔式编排）— ⚠️ **重大发现：已被并行工作流重写为 v2**
- **发现**：调研开始时我按 git HEAD（v1：tower_create/dispatch/status 分层编排，Kimi /tower 推断实现）做静态 review；实测阶段发现 **dsh web 实例（:20010）中的并行会话已于 2026-08-16 16:41-16:59 将 dsh-tower 重写为 v2**：忠实移植 Kimi Code **上游 2026-08-16 刚合并的 /tower**（kimi-code #2633，control-tower 模型）——11 个工具（tower_init/plan/spawn/status/send/inbox/mission/finding/review/merge/teardown），`.tower/` 协议存储 + git worktree + review/merge 门禁 + 活动日志。旧 REPORT.md 中「Kimi /tower 不存在」的推断已过时。
- **实测（v2，我独立复测）**：`/tmp/tower-review-repo` 初始化 → tower_init → tower_plan（1 mission）→ tower_spawn（worker 入 worktree）→ 25s 后 worker 完成：welcome.txt 内容 TOWER-V2-OK-88、git 提交 ec0c16d、任务勾选 + note + 广播「M1 完成」，全链路 15s 完成。
- **Review 发现（已上游实证闭环）**：worker 完成全部任务并广播后，`tower_mission` 仍显示 `status=active`——初判"可能遗漏"，后核验上游 kimi-code #2633（commit f492cd7c）协议源码 `protocol/store.ts`：`mission.update` 的 status 仅在显式 `patch.status` 时改变，`taskDone` 只勾任务、绝不自动流转；且工作流文档 tower.md 明确 mission 的**操作终态是 `merged`**（TowerMerge 置 status='merged'，全部 merged 后才 teardown）。**结论：worker 完成后 mission 保持 active 是忠实上游的设计**，由 tower 监督阶段收尾（review→merge），非缺陷。②`tower_teardown`/`tower_merge` 未在本轮实测覆盖（并行会话 e2e 已跑 merge/review 部分）；③注入顺序 `["tools","skills","subagents","agents"]` 无 timer（该插件不用 interval，正确）。

### 5.5 dsh-kanban（Hermes 风格看板）— ✅ 实测通过（全生态领先）
- **实测**：建板 → 建任务 → 流转 ready → 派发 → **子代理真实执行并输出 KANBAN-REAL-OK-2026** → 自动结算 done + 摘要回写 + 事件时间线。
- **静态审查**：SQLite WAL 持久；dispatcher 三职责（心跳回收 30min / 依赖晋升 / 自动派发）；heartbeat 挂在 session/event 上（优雅）。**发现**：①任务派发依赖活 parent agent（headless 单次运行内有效，进程退出子代理即终止——REPORT 已注明，属已知限制）；②`kanban_update_task` 把 running 任务移走时 abort 信号但未等子代理真正退出（可接受）；③无任务级「审批流」列（生态空白项，可后续加 review 列审批）。

### 5.6 dsh-tui-app（Ink TUI）— ✅ PTY 实测通过；**/quit 挂死 bug 已修复并推送（210d500）**
- **实测**（真 PTY）：启动 → 鲸鱼开屏 + figlet DSH + 信息面板（deepseek-v4-flash / PTC 模式 / t0 s0 / cwd）→ 发送消息 → **🤔 思考折叠块 + ⚙ bash ✓ 工具卡片 + 流式回答**（TUI-REAL-OK-99）→ 状态栏实时更新（t1 · s2 · llm 2.6s · tool 74ms · cache 52% · tps 231 · tok 35.7k）。
- **Review 发现并修复：/quit 挂死**（复现：/quit → "bye." → 进程永不退出，仅 Ctrl+D 可脱身）。根因两层：①`exit()` 先 `await agent-handle dispose()` 再调 `io.exit(0)`——agent teardown 挂起时 launcher 的有界 shutdown（5s 强制退出兜底）根本不会启动；②即使 io.exit 执行、tree dispose 完成、exitCode 置 0，**raw-mode resume 后无 EOF 的 stdin 句柄仍永久撑住事件循环**。修复：`io.exit(0)` 提前调用 + `dispose().catch()` + 显式 `stdin.pause()/destroy()` + unref'd 3s 硬退出安全网。**回归验证**：启动→对话（TUI-QUIT-REGRESSION-OK 执行成功）→ `/quit` 单独自然退出（exit_code 0），已提交并推送 github.com/kouyichi/dsh-tui-app。
- **其余审查**：代码 3499 行（lib/ 模块化：store/runtime/input/markdown/theme/channel 分层清晰）；Ink effects 不可靠→外部 store + rerender 直驱、50ms 事件批量、markdown token 化渲染等技能文档记载的模式均在代码中落实。

### 5.7 全家桶横向审查
- **一致性问题**：旧 REPORT.md（v1 tower 描述）已被并行会话的 IMPLEMENTATION-REPORT.md 取代；建议后续以 git log 为准。
- **重复模式**：五个 v1 插件各自复制了 `pickProvider`/`pickParent`/`present` 帮助函数（~50 行/份）——可提取共享 lib（低优先级，插件独立性优先）。
- **安全**：所有插件存储均在 `$DSH_HOME` 下；无密钥写入（dream prompt 明确禁止）；guard 默认零规则。
- **测试**：v1 插件无单元测试（实测覆盖）；新插件 verify.mjs 模式（mock-ctx）建议回填到 v1 插件。

---

## 6. 实测证据清单（可复现）

| 插件 | 证据 | 位置 |
|---|---|---|
| scaffold | 生成+验证+兼容检查输出 | `/tmp` 清理前已记录；verify.mjs 模式在 dsh-scaffold/ 内 |
| guard | 拒绝事件+审计报告 | `~/.dsh/guard/audit.jsonl`（177 条，含 denied 事件） |
| xray | 50 会话分析+导出文件 | `~/.dsh/xray/session-*.md` |
| cron | 真实执行日志 | `~/.dsh/cron/logs.jsonl`（3 条：2 失败+1 成功） |
| bench | 2/2 通过报告 | `~/.dsh/bench/runs/run-2026-08-16T08-59-31/` |
| pack | smoke-pack | `~/.dsh/packs/smoke-pack.md` |
| a2a | curl 全链路 | 会话记录（tasks/send→completed，A2A-REAL-OK-777） |
| meter | 用量报告 | `~/.dsh/meter/reports/meter-2026-08-16T08-56-44.md` |
| learn/dream/tower/kanban/profile/tui | 见 §5 各实测输出 | 会话均落盘 `~/.dsh/sessions/`（xray 可查） |

测试残留已清理：guard 测试规则、cron smoke 任务、kanban review-board、/tmp 测试仓库（learn 的 review-smoke 保留在 archived 作为可恢复证据）。

---

## 7. 结论与建议

1. **8 个新插件全部完成调研→开发→验证→实测闭环**，挂载后全 profile 可用；连同既有 5 个 + tui-app，本机形成 **13 插件 + 1 TUI 的 dsh 插件全家桶**，覆盖生态 7 大空白中的 6 个（kanban 闭环/安全治理/工具链/评测/会话分析/定时任务）+ A2A 差异化。
2. **生态窗口**：dsh 生态才 3 天，官方 API 仍在 breaking change；本机插件已验证 rc.6 契约，建议尽快按 dsh-tui-app 先例发布到 GitHub（kouyichi/dsh-plugins），抢占工具链/治理/评测三个「工具型」空白——这类插件对 API 变动最敏感，先发者优势最大。
3. **遗留事项**：①tower v2 的 mission 自动收尾语义待与用户确认；②dream 的 `last===null && MEMORY.md 存在` 跳过 bug 建议修复；③tui `/quit` PTY 场景核查；④v1 插件补 verify.mjs；⑤插件代码推 GitHub（与并行会话协调，避免覆盖其 tower v2 工作）。
4. **技能沉淀**：本次踩坑（ESM 真实路径解析、ctx.on disposer、guard API、插件自主建 agent 模式、A2A server 模式）已全部写入 `dsh-harness-development` 技能。

---

## 附录 A：Git 状态

```
4600c6d fix: cordis ctx.on disposer / scaffold stripper / pack NaN / plugin root deps / install.sh 13 plugins   ← 本次
929abbd report: tower v2 faithful port (kimi-code #2633), 44 tools total                                        ← 并行会话
35521e3 dsh-tower v2: faithful port of Kimi Code /tower (control-tower model)                                  ← 并行会话
7760cf9 fix: lazy DSH_HOME resolution in dsh-profile core
5ee1e06 fix result extraction, timer inject, report
86fd630 dsh plugin suite: learn/profile/dream/tower/kanban
```

## 附录 B：调研方法限制
- 本机网络白名单（github/npm/raw 可达，google/Firecrawl 不可用）→ 全部用 gh api + npm registry + raw.githubusercontent.com 实测数据，无二手转述。
- star 数为 2026-08-16 快照，生态爆发期变化极快。
- claude-code/pi/opencode 的外部文档站无法直连，以其仓库内文档/源码为准（报告中已标注）。
